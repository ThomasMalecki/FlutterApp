import 'package:flutter/material.dart';

class AvailableScreen extends StatelessWidget {
  const AvailableScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Available Screen'),
    );
  }
}
